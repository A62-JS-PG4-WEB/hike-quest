/**
 * NotAuthorized component that displays a message indicating that the user
 * does not have the necessary permissions to access a particular page or resource.
 *
 * @component
 * @example
 * return (
 *   <NotAuthorized />
 * )
 */
export default function NotAuthorized() {
  return (
      <div>
          <h1>No permissions!</h1>
      </div>
  );
}